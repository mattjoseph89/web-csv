<!DOCTYPE html>
<html>
<head>

  <title>All Accounts</title>
</head>
<body>
  <%accounts.each do |name, info| %>
  <table>
    <tr>
      <th>Account: <%= name%> ... Balance: $<%=info[:tally].round(2) %></th>
      <th> </th>
      <th> </th>
    </tr>
    <tr>
      <th>Category</th>
      <th>Total Spent</th>
      <th>Average Transaction</th>
    </tr><%info[:categories].each do |category, c_info| %>
    <tr>
      <td><%= category %></td>
      <td>$<%= c_info[:tally].round(2).to_s %></td>
      <td>$<%= c_info[:average_transaction_cost].round(2).to_s %></td>
    </tr><%end%>
  </table><%end%>
</body>
</html>
