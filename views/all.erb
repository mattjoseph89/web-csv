<!DOCTYPE html>
<html lang="en-us">

<head>  
<meta charset="utf-8">
<title>all accounts</title>
<link rel="stylesheet" type="text/css" href="/style.css">
<link href="https://fonts.googleapis.com/css?family=Raleway:700i|Roboto+Mono:500" rel="stylesheet">
</head> 

<body class="wrapper">
  <% accounts.each do |name, info| %>
  <table>
    <tr>
      <th colspan="3">Account: <%= name %> | Balance: $<%= info[:tally].round(2) %></th>
    </tr>
    <tr>
      <th>Category</th>
      <th>Total Spent</th>
      <th>Average Transaction</th>
    </tr><center><% info[:categories].each do |category, c_info| %>
    </center><tr>
      <td><%= category %></td>
      <td><center>$<%= c_info[:tally].round(2).to_s %></center></td>
      <td><center>$<%= c_info[:average_transaction_cost].round(2).to_s %></center></td>
    </tr><% end %>
  </table><% end %>

      
    <a href="/add_row_form">
      <button>Add Transaction</button>

     <a href="/main">
      <button>Home</button>

    <a href="/log_out">
      <button>Logout</button>
</body>
</html>
